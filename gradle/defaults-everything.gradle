allprojects {
  apply plugin: 'base'

  group "org.apache"

  repositories {
    mavenCentral()

    maven {
      url "https://maven.restlet.com"
    }
  }

  project.archivesBaseName = project.path.replaceAll("^:", "").replace(':', '-')
}
